#include "math.h"

BYTE minByte(BYTE a, BYTE b) {
	return (a < b) ? a : b;
}

UBYTE minUByte(UBYTE a, UBYTE b) {
	return (a < b) ? a : b;
}

WORD minWord(WORD a, WORD b) {
	return (a < b) ? a : b;
}

UWORD minUWord(UWORD a, UWORD b) {
	return (a < b) ? a : b;
}

LONG minLong(LONG a, LONG b) {
	return (a < b) ? a : b;
}

ULONG minULong(ULONG a, ULONG b) {
	return (a < b) ? a : b;
}

BYTE maxByte(BYTE a, BYTE b) {
	return (a < b) ? a : b;
}

UBYTE maxUByte(UBYTE a, UBYTE b) {
	return (a < b) ? a : b;
}

WORD maxWord(WORD a, WORD b) {
	return (a < b) ? a : b;
}

UWORD maxUWord(UWORD a, UWORD b) {
	return (a < b) ? a : b;
}

LONG maxLong(LONG a, LONG b) {
	return (a < b) ? a : b;
}

ULONG maxULong(ULONG a, ULONG b) {
	return (a < b) ? a : b;
}